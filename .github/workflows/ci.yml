name: Playwright Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Playwright
        uses: playwright/github-action@v2
      - name: Run Playwright tests
        run: npx playwright test
